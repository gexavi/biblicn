# Dépendances (réinstallées via npm install / au build Docker)
node_modules/
package-lock.json

# Données personnelles : base de données, couvertures téléchargées, exports CSV
# -> ne doit JAMAIS être versionné (c'est VOTRE bibliothèque, pas du code)
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Identifiants locaux (voir .env.example pour le modèle à copier)
# -> ne doit JAMAIS être versionné, contient AUTH_USERNAME/AUTH_PASSWORD en clair
.env

# Fichiers système
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
